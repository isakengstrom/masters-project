import torch
import torchvision
from torch.utils.data import Dataset, DataLoader
import numpy as np


class FOIDataset(Dataset):
    def __init__(self):
        # Data loading
        print()

    def __getitem__(self, index):
        # Dataset[0]
        print()

    def __len__(self):
        # len(dataset)
        print()